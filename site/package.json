{
  "name": "cevolta",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@11.13.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
